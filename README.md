Para ejecutar la automatizacion se tiene que ejecutar el build.gradle 
seguido ir al archivo de java TestParallel y ejecutarlo, como segunda opcion se pueden ejecutar los runners de cada feature en el siguiente orden:
add_pet.feature
find_pet_by_id.feature
update_pet.feature
find_pet_by_status.feature
